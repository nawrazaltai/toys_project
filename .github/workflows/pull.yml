name: Mirror and run GitLab CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Mirror + trigger CI
      uses: SvanBoxel/gitlab-mirror-and-ci-action@master
      with:
        args: "https://gitlab.com/hardi.kakalaw/toys-projec.git"
      env:
        FOLLOW_TAGS: "false"
        FORCE_PUSH: "false"
        GITLAB_HOSTNAME: "gitlab.com"
        GITLAB_USERNAME: "hardi.kakalaw"
        GITLAB_PASSWORD: "glpat-vuoa3nM5MXRtdm9mWTbi"
        GITLAB_PROJECT_ID: "46033255" 
        GITHUB_TOKEN: "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"
